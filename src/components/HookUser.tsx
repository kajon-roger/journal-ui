import React, { useState, useEffect, useReducer, useRef, useCallback, useContext } from "react";

interface Props {

}

export function HookUser(props: Props) {

    return (
        <div className="container-fluid">
            This Component Uses Hooks!
        </div>
    );
}